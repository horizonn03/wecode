### 1. Git Repository 생성 & Git 폴더 초기화

- git init: 해당폴더에 git을 사용할 수 있도록 초기화

### 2. Git 폴더에 파일 생성 & 내용 작성 및 Repository에 push

- git remote -v origin + (repository 주소): git directory와 repository 연동
- git add. -> git commit +(커밋 메세지) -> git push
  s
